apiVersion: v1
kind: Service
metadata:
  name: cinemora-backend-service
  namespace: cinemora
  labels:
    app: cinemora-backend
spec:
  selector:
    app: cinemora-backend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5001
  type: LoadBalancer
